---

- name: Deploy the latest Flask app
  hosts: localhost
  tasks:

  - name: Stop the running container
    docker_container:
     name: flask
     state: stopped
     image: kalpana901/flask

  - name: Remove the stopped container
    docker_container:
     name: flask
     state: absent

  - name: Pull the latest image from docker hub
    docker_image:
     name: kalpana901/flask
     tag: latest
     source: pull

  - name: Run the container with the latest image
    docker_container:
     name: flask
     image: kalpana901/flask:latest
     state: started
     detach: yes
     tty: yes
     ports: [8000:7000]
     restart_policy: always
